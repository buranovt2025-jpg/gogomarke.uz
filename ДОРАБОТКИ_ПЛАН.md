# 📋 ПЛАН ДОРАБОТОК GoGoMarket

**Дата:** 2026-01-16  
**Приоритет:** По важности

---

## 🔴 ЭТАП 1: КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ

### 1.1 Исправить корзину (Cart)
**Файлы для изменения:**
- `backend/src/controllers/cartController.ts`
- `backend/src/models/CartItem.ts`

**Действия:**
1. Проверить соответствие полей модели CartItem и таблицы cart_items
2. Добавить логирование ошибок в контроллер
3. Тестировать API:
   - POST /api/v1/cart/items
   - GET /api/v1/cart
   - PUT /api/v1/cart/items/:id
   - DELETE /api/v1/cart/items/:id

### 1.2 Исправить Video API
**Файлы для изменения:**
- `backend/src/controllers/videoController.ts`
- `backend/src/models/Video.ts`

**Действия:**
1. Добавить try-catch с детальным логированием
2. Проверить модель Video на соответствие БД
3. Тестировать API:
   - GET /api/v1/videos
   - GET /api/v1/videos/:id
   - POST /api/v1/videos/:id/comments

### 1.3 Исправить Frontend авторизацию
**Файлы для изменения:**
- `web/gogomarket-web/src/services/api.ts`
- `web/gogomarket-web/src/contexts/AuthContext.tsx`

**Действия:**
1. Убедиться что setToken сохраняет в localStorage
2. Добавить логирование в AuthContext
3. Проверить формат ответа от /auth/login
4. Тестировать:
   - Регистрация → автологин
   - Логин → сохранение сессии
   - Обновление страницы → сохранение сессии

---

## 🟡 ЭТАП 2: СРЕДНИЕ УЛУЧШЕНИЯ

### 2.1 Добавить Seller Reports
**Файлы для изменения:**
- `backend/src/controllers/sellerController.ts`
- `backend/src/routes/sellerRoutes.ts`

**Новые endpoints:**
```typescript
// GET /api/v1/seller/reports?period=day|week|month
router.get('/reports', authenticate, authorize('seller'), getSellerReports);

// GET /api/v1/seller/reports/export?startDate=...&endDate=...
router.get('/reports/export', authenticate, authorize('seller'), exportSellerReports);
```

### 2.2 Улучшить QR-сканирование
**Файлы для изменения:**
- `backend/src/controllers/orderController.ts`
- `backend/src/services/qrService.ts`

**Действия:**
1. Добавить endpoint для получения QR-данных в JSON:
   ```
   GET /api/v1/orders/:id/qr-data
   ```
2. Добавить альтернативный способ подтверждения (код вместо QR)

### 2.3 Комментарии с ответами
**Миграция:**
```sql
ALTER TABLE comments ADD COLUMN parent_id UUID REFERENCES comments(id);
CREATE INDEX idx_comments_parent_id ON comments(parent_id);
```

**Файлы для изменения:**
- `backend/src/models/Comment.ts`
- `backend/src/controllers/videoController.ts`

---

## 🟢 ЭТАП 3: ОПТИМИЗАЦИИ

### 3.1 Frontend Performance
- Добавить React.lazy() для маршрутов
- Оптимизировать bundle size
- Добавить image lazy loading

### 3.2 Admin Dashboard
- Добавить графики (Chart.js или Recharts)
- Экспорт отчетов в PDF/Excel
- Real-time обновления через WebSocket

### 3.3 Mobile Optimization
- PWA support
- Offline mode
- Push notifications

---

## ⏱️ ОЦЕНКА ВРЕМЕНИ

| Этап | Задачи | Оценка |
|------|--------|--------|
| 1.1 | Корзина | 2-3 часа |
| 1.2 | Video API | 2-3 часа |
| 1.3 | Frontend Auth | 3-4 часа |
| 2.1 | Seller Reports | 4-5 часов |
| 2.2 | QR улучшения | 2-3 часа |
| 2.3 | Комментарии | 2-3 часа |
| 3.x | Оптимизации | 8-10 часов |

**Итого этап 1:** ~8-10 часов  
**Итого этап 2:** ~8-11 часов  
**Итого этап 3:** ~8-10 часов

---

## ✅ ЧЕКЛИСТ ПЕРЕД РЕЛИЗОМ

- [ ] Все критические баги исправлены
- [ ] Все роли протестированы E2E
- [ ] Frontend auth работает стабильно
- [ ] Корзина работает
- [ ] Видео работает
- [ ] Чат работает
- [ ] Заказы работают полный цикл
- [ ] Транзакции создаются автоматически
- [ ] Админ панель работает
- [ ] HTTPS настроен
- [ ] Production environment variables установлены
- [ ] Резервное копирование БД настроено

---

## 🚀 ГОТОВО К РАБОТЕ

Начать с **Этапа 1**, исправить критические баги, затем переходить к следующим этапам.
