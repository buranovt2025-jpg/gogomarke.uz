# Инструкция для следующего чата: Запуск GoGoMarket.uz

## Контекст проекта

**GoGoMarket.uz** — социальный маркетплейс с видео-контентом (TikTok Shop clone).

### Технический стек
| Компонент | Технология |
|-----------|------------|
| Backend | Node.js + Express + TypeScript + Sequelize |
| Database | PostgreSQL |
| Web Frontend | React + Vite + TypeScript + Tailwind CSS |
| Mobile Frontend | Flutter (Dart) |
| File Storage | DigitalOcean Spaces (S3) |
| Server | DigitalOcean Droplet (64.226.94.133) |

---

## Текущий статус: 98% готовности ✅

### Что сделано:
- ✅ Backend API полностью готов (27 контроллеров, 24 модели)
- ✅ Централизованная финансовая логика (`financeService.ts`)
- ✅ State Machine для статусов заказов (`orderStateMachine.ts`)
- ✅ SQL миграции (`backend/migrations/001_initial_schema.sql`)
- ✅ Web-приложение (React)
- ✅ Mobile-приложение (Flutter)
- ✅ TypeScript компилируется без ошибок

### Что осталось:
- ⏳ Установить Node.js на локальной машине
- ⏳ Запустить backend сервер
- ⏳ Выполнить миграции в PostgreSQL

---

## Проблема: Node.js не установлен

Если при выполнении команд вы видите:
```
'node' is not recognized as an internal or external command
```
или
```
npm: command not found
```

**Решение:** Установите Node.js

---

## Пошаговая инструкция

### Шаг 1: Установить Node.js LTS

1. Перейдите на https://nodejs.org/
2. Скачайте **LTS версию** (рекомендуется)
3. Запустите установщик
4. ✅ Отметьте "Add to PATH" при установке
5. Перезагрузите терминал/компьютер

### Шаг 2: Проверить установку

```bash
node --version
# Ожидается: v20.x.x или выше

npm --version
# Ожидается: 10.x.x или выше
```

### Шаг 3: Установить зависимости Backend

```bash
cd backend
npm install
```

Ожидаемый результат:
```
added 500+ packages in X seconds
```

### Шаг 4: Настроить переменные окружения

```bash
# Скопировать пример конфигурации:
cp .env.example .env

# Отредактировать .env файл:
# DATABASE_URL=postgresql://user:password@localhost:5432/gogomarket
# JWT_SECRET=your-secret-key
```

### Шаг 5: Запустить Backend

```bash
npm run dev
```

Ожидаемый результат:
```
Server is running on port 3000
Database connection established successfully.
```

### Шаг 6: Проверить работу API

```bash
curl http://localhost:3000/api/v1/health
```

Ожидаемый ответ:
```json
{"success":true,"message":"API is running"}
```

---

## Команды для копирования

```bash
# Установка и запуск backend
cd backend
npm install
npm run dev

# Проверка API
curl http://localhost:3000/api/v1/health

# Сборка TypeScript
npm run build

# Проверка типов
npx tsc --noEmit

# Установка и запуск web frontend
cd web/gogomarket-web
npm install
npm run dev
# Открыть http://localhost:5173

# Установка и запуск mobile
cd frontend
flutter pub get
flutter run
```

---

## Troubleshooting

### Ошибка: "node is not recognized"
**Решение:** 
1. Переустановите Node.js с https://nodejs.org/
2. Убедитесь, что отметили "Add to PATH"
3. Перезагрузите терминал

### Ошибка: "EACCES permission denied"
**Решение (Linux/Mac):**
```bash
sudo npm install
# или настройте npm без sudo:
npm config set prefix ~/.npm-global
```

### Ошибка: "Cannot connect to database"
**Решение:**
1. Убедитесь, что PostgreSQL запущен
2. Проверьте DATABASE_URL в .env
3. Создайте базу данных:
```sql
CREATE DATABASE gogomarket;
```

### Ошибка: "Port 3000 already in use"
**Решение:**
```bash
# Найти процесс:
lsof -i :3000  # Mac/Linux
netstat -ano | findstr :3000  # Windows

# Убить процесс или использовать другой порт:
PORT=3001 npm run dev
```

---

## Чек-лист выполнения

- [ ] Node.js установлен (`node --version` работает)
- [ ] npm установлен (`npm --version` работает)
- [ ] Зависимости установлены (`npm install` успешно)
- [ ] .env файл настроен
- [ ] Backend запускается (`npm run dev`)
- [ ] API отвечает (`/api/v1/health` возвращает 200)
- [ ] PostgreSQL подключен (если нужна БД)

---

## Структура Backend

```
backend/
├── src/
│   ├── app.ts                 # Express приложение
│   ├── config/
│   │   ├── database.ts        # Sequelize конфигурация
│   │   └── index.ts           # Общие настройки
│   ├── controllers/           # 27 контроллеров
│   │   ├── orderController.ts
│   │   ├── adminController.ts
│   │   ├── authController.ts
│   │   └── ...
│   ├── models/                # 24 модели Sequelize
│   │   ├── User.ts
│   │   ├── Product.ts
│   │   ├── Order.ts
│   │   └── ...
│   ├── services/              # Бизнес-логика
│   │   ├── financeService.ts  # Финансовые расчеты
│   │   └── orderStateMachine.ts
│   ├── routes/                # Express маршруты
│   └── types/                 # TypeScript типы
├── migrations/
│   └── 001_initial_schema.sql # SQL миграции
├── package.json
└── tsconfig.json
```

---

## Формат отчета после выполнения

```
## Отчет о выполнении

### Node.js
- Версия: [v20.x.x]
- Статус: [установлен/не установлен]

### Backend
- Статус: [работает/не работает]
- Порт: [3000]
- /health: [OK/Error]

### База данных
- PostgreSQL: [подключен/не подключен]
- Миграции: [выполнены/не выполнены]

### Ошибки
- [список ошибок, если есть]

### Следующие шаги
- [рекомендации]
```

---

## Контакты и ресурсы

- **Репозиторий:** https://github.com/buranovt2025-jpg/gogomarke.uz
- **Ветка:** `cursor/-bc-c5dbac09-4d95-4ec5-822a-3e2053ac7ad7-924d`
- **Сервер:** 64.226.94.133
- **Web URL:** https://64-226-94-133.sslip.io/
- **Node.js:** https://nodejs.org/

---

## Быстрый старт (TL;DR)

```bash
# 1. Установите Node.js с https://nodejs.org/

# 2. Проверьте установку
node --version && npm --version

# 3. Запустите backend
cd backend && npm install && npm run dev

# 4. Проверьте API
curl http://localhost:3000/api/v1/health
```

**Готово!** 🎉
